root: "E:/fashion-data" # e.g. "E:/fashion-data"
raw: "raw" # raw images
npy: "npy" # preprocessed .npy arrays
norm: "norm" # normalization files (.npy)
weights: "weights" # saved checkpoints
csv: "csv" # label CSV files
logs: "logs" # MLflow and other logs
